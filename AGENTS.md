Always use interfaces over types in TypeScript.
This project has a React compiler enabled, so do not use optimisation hooks.
This is a greenfield project, so do not preserve backwards compatibility unless the user explicitly asks for it.
When making Convex architecture decisions, inspect and follow the Convex patterns used in /Users/shahbaz/dev/dryft where they fit this codebase.
